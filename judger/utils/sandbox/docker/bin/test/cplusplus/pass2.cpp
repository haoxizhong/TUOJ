#include<iostream>
#include<cstring>
#include<algorithm>
#include<cstdio>
#include<cmath>
using namespace std;
#define MAXN 1000
int a[MAXN];

int main()
{
		for (int i=0;i<100000000;i++);
		cout<<"HelloWorld\n";
		memset(a,0,sizeof(a));
		int *ptr = new(int);
		int *ptr2 = (int*)malloc(100*sizeof(int));
		cout<<sqrt(4)<<" "<<asin(0.5)<<endl;
		for (int i=0;i<100000000;i++);
}
